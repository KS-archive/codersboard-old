# import * from './generated/prisma.graphql'

type Message {
  message: String
}

type Mutation {
  signUp(email: String!, password: String, image: String!, fullName: String!): User!
  signIn(email: String!, password: String!): User!
  signOut: Message
}

type Query {
  me: User
  users: [User!]
}
