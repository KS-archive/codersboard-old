mutation createCredential {
  createCredential(
    data: {
      name: "Name of the site"
      login: "some_login"
      password: "some_password"
    }
  ) {
    id
    name
  }
}

query credentials {
  credentials {
    id
    name
    login
    password
  }
}
