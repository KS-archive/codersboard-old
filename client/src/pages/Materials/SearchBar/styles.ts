import styled from 'styled-components';
import get from 'styles/getStyle';

export const SearchBarContainer = styled.div`
  display: flex;
  align-items: flex-start;
  width: 75%;
  margin-bottom: ${get("space-24")};
  padding-right: ${get("space-8")};
`;
